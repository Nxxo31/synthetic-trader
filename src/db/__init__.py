"""Database layer — SQLite persistence for the Pradx strategy store.

Modules:
    migration_001  — initial schema (6 tables) + indexes
    service         — service layer (Strategy, Performance, Regime, Optimization)
"""
